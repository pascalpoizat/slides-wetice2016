DECLARATIONS

    component spec chorD
        [x>=0] |> confirm[a,b].x + [x<0] |> cancel[a,b].x
    end component

    component impl chorD
        confirm[a,b].x
    end component

COMMANDS

    showSTG spec impl
    conformance impl spec
